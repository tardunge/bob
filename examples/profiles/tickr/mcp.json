{
  "mcpServers": {
    "bob-memory": {
      "command": "node",
      "args": ["${BOB_REPOSITORY_ROOT}/examples/mcp/conversation-memory.mjs"],
      "env": {
        "DATABASE_PATH": "${DATABASE_PATH}"
      }
    }
  }
}
